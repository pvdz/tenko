# Tenko parser test case

- Path: tests/testcases/regexes/v_flag/subtraction_then_range_trap_v_pass.md

> :: regexes : v flag
>
> ::> subtraction then range trap v pass
>
> A ClassSetExpression is exclusively a union, an `&&` chain or a `--` chain (ES2024 22.2.1); after `a--b` a bare range `-c` mixes union syntax into the subtraction chain
## FAIL

## Input

- `es = 15`

`````js
/[a--b-c]/v
`````

## Output

_Note: the whole output block is auto-generated. Manual changes will be overwritten!_

Below follow outputs in five parsing modes: sloppy, sloppy+annexb, strict script, module, module+annexb.

Note that the output parts are auto-generated by the test runner to reflect actual result.

### Sloppy mode

Parsed with script goal and as if the code did not start with strict mode header.

`````
throws: Lexer error!
    Regex: With the v flag, a range is only valid in a union, not in an `&&` / `--` chain

start@1:0, error@1:0
╔══╦════════════════
 1 ║ /[a--b-c]/v
   ║ ^^^^^^^^^^^------- error
╚══╩════════════════

`````

### Strict mode

Parsed with script goal but as if it was starting with `"use strict"` at the top.

_Output same as sloppy mode._

### Module goal

Parsed with the module goal.

_Output same as sloppy mode._

### Sloppy mode with AnnexB

Parsed with script goal with AnnexB rules enabled and as if the code did not start with strict mode header.

_Output same as sloppy mode._

### Module goal with AnnexB

Parsed with the module goal with AnnexB rules enabled.

_Output same as sloppy mode._
